<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : email_html_template
  Comment  : 网页邮件模板
  Mapper   : com.welearn.mapper.EmailHtmlTemplateMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2018/11/27 10:16:00
-->
<mapper namespace="com.welearn.mapper.EmailHtmlTemplateMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    code,
    name,
    description,
    html,
    args
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    email_html_template.id AS id,
    email_html_template.createdAt AS createdAt,
    email_html_template.updatedAt AS updatedAt,
    email_html_template.isEnable AS isEnable,
    email_html_template.code AS code,
    email_html_template.name AS name,
    email_html_template.description AS description,
    email_html_template.html AS html,
    email_html_template.args AS args
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.notify.EmailHtmlTemplate" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 模板类型 -->
    <result column="code" property="code"/>
    <!-- Comment: 模板名称 -->
    <result column="name" property="name"/>
    <!-- Comment: 模板描述 -->
    <result column="description" property="description"/>
    <!-- Comment: 模板内容 -->
    <result column="html" property="html"/>
    <!-- Comment: 参数描述 -->
    <result column="args" property="args"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.notify.EmailHtmlTemplate" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM email_html_template
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.notify.EmailHtmlTemplate" parameterType="com.welearn.entity.qo.notify.EmailHtmlTemplateQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM email_html_template
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="code != null" >
          AND code = #{code}
        </if>
        <if test="name != null" >
          AND name = #{name}
        </if>
        <if test="description != null" >
          AND description = #{description}
        </if>
        <if test="html != null" >
          AND html = #{html}
        </if>
        <if test="args != null" >
          AND args = #{args}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.notify.EmailHtmlTemplate" >
    SELECT
      <include refid="sqlColumns"/>
    FROM email_html_template
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM email_html_template
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.notify.EmailHtmlTemplate" >
    UPDATE email_html_template
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      code = #{code},
      name = #{name},
      description = #{description},
      html = #{html},
      args = #{args},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.notify.EmailHtmlTemplate" >
    UPDATE email_html_template
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="code != null" >
        code = #{code},
      </if>
      <if test="name != null" >
        name = #{name},
      </if>
      <if test="description != null" >
        description = #{description},
      </if>
      <if test="html != null" >
        html = #{html},
      </if>
      <if test="args != null" >
        args = #{args},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE email_html_template SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE email_html_template SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.notify.EmailHtmlTemplate" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO email_html_template
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      code,
      name,
      description,
      html,
      args,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{code},
      #{name},
      #{description},
      #{html},
      #{args},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.notify.EmailHtmlTemplate" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO email_html_template 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="code != null" >
        code,
      </if>
      <if test="name != null" >
        name,
      </if>
      <if test="description != null" >
        description,
      </if>
      <if test="html != null" >
        html,
      </if>
      <if test="args != null" >
        args,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="code != null" >
        #{code},
      </if>
      <if test="name != null" >
        #{name},
      </if>
      <if test="description != null" >
        #{description},
      </if>
      <if test="html != null" >
        #{html},
      </if>
      <if test="args != null" >
        #{args},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM email_html_template WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM email_html_template
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>