<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : re_task_package
  Comment  : 库存任务与货物包装关联
  Mapper   : com.welearn.mapper.ReTaskPackageMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2018/12/18 16:19:44
-->
<mapper namespace="com.welearn.mapper.ReTaskPackageMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    taskId,
    packageId,
    packageCount
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    re_task_package.id AS id,
    re_task_package.createdAt AS createdAt,
    re_task_package.updatedAt AS updatedAt,
    re_task_package.isEnable AS isEnable,
    re_task_package.taskId AS taskId,
    re_task_package.packageId AS packageId,
    re_task_package.packageCount AS packageCount
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.finance.ReTaskPackage" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 任务id -->
    <result column="taskId" property="taskId"/>
    <!-- Comment: 包装id -->
    <result column="packageId" property="packageId"/>
    <!-- Comment: 包装内含数量 -->
    <result column="packageCount" property="packageCount"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.finance.ReTaskPackage" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM re_task_package
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.finance.ReTaskPackage" parameterType="com.welearn.entity.qo.finance.ReTaskPackageQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM re_task_package
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="taskId != null" >
          AND taskId = #{taskId}
        </if>
        <if test="packageId != null" >
          AND packageId = #{packageId}
        </if>
        <if test="packageCount != null" >
          AND packageCount = #{packageCount}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.finance.ReTaskPackage" >
    SELECT
      <include refid="sqlColumns"/>
    FROM re_task_package
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM re_task_package
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.finance.ReTaskPackage" >
    UPDATE re_task_package
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      taskId = #{taskId},
      packageId = #{packageId},
      packageCount = #{packageCount},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.finance.ReTaskPackage" >
    UPDATE re_task_package
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="taskId != null" >
        taskId = #{taskId},
      </if>
      <if test="packageId != null" >
        packageId = #{packageId},
      </if>
      <if test="packageCount != null" >
        packageCount = #{packageCount},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE re_task_package SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE re_task_package SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.finance.ReTaskPackage" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_task_package
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      taskId,
      packageId,
      packageCount,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{taskId},
      #{packageId},
      #{packageCount},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.finance.ReTaskPackage" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_task_package 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="taskId != null" >
        taskId,
      </if>
      <if test="packageId != null" >
        packageId,
      </if>
      <if test="packageCount != null" >
        packageCount,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="taskId != null" >
        #{taskId},
      </if>
      <if test="packageId != null" >
        #{packageId},
      </if>
      <if test="packageCount != null" >
        #{packageCount},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM re_task_package WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM re_task_package
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>