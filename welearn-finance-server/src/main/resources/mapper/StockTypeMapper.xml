<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : stock_type
  Comment  : 库存类型
  Mapper   : com.welearn.mapper.StockTypeMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2018/12/17 15:06:27
-->
<mapper namespace="com.welearn.mapper.StockTypeMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    type,
    companyId,
    itemTypeId,
    itemTypeName,
    specification,
    description,
    unit
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    stock_type.id AS id,
    stock_type.createdAt AS createdAt,
    stock_type.updatedAt AS updatedAt,
    stock_type.isEnable AS isEnable,
    stock_type.type AS type,
    stock_type.companyId AS companyId,
    stock_type.itemTypeId AS itemTypeId,
    stock_type.itemTypeName AS itemTypeName,
    stock_type.specification AS specification,
    stock_type.description AS description,
    stock_type.unit AS unit
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.finance.StockType" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 对象类型:0-医疗设备 1-设备备件 2-医用耗材 -->
    <result column="type" property="type"/>
    <!-- Comment: 公司id -->
    <result column="companyId" property="companyId"/>
    <!-- Comment: 类型id -->
    <result column="itemTypeId" property="itemTypeId"/>
    <!-- Comment: 类型名称 -->
    <result column="itemTypeName" property="itemTypeName"/>
    <!-- Comment: 类型规格 -->
    <result column="specification" property="specification"/>
    <!-- Comment: 类型描述 -->
    <result column="description" property="description"/>
    <!-- Comment: 基本单位 -->
    <result column="unit" property="unit"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.finance.StockType" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM stock_type
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.finance.StockType" parameterType="com.welearn.entity.qo.finance.StockTypeQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM stock_type
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="type != null" >
          AND type = #{type}
        </if>
        <if test="companyId != null" >
          AND companyId = #{companyId}
        </if>
        <if test="itemTypeId != null" >
          AND itemTypeId = #{itemTypeId}
        </if>
        <if test="itemTypeName != null" >
          AND itemTypeName = #{itemTypeName}
        </if>
        <if test="specification != null" >
          AND specification = #{specification}
        </if>
        <if test="description != null" >
          AND description = #{description}
        </if>
        <if test="unit != null" >
          AND unit = #{unit}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.finance.StockType" >
    SELECT
      <include refid="sqlColumns"/>
    FROM stock_type
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM stock_type
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.finance.StockType" >
    UPDATE stock_type
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      type = #{type},
      companyId = #{companyId},
      itemTypeId = #{itemTypeId},
      itemTypeName = #{itemTypeName},
      specification = #{specification},
      description = #{description},
      unit = #{unit},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.finance.StockType" >
    UPDATE stock_type
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="type != null" >
        type = #{type},
      </if>
      <if test="companyId != null" >
        companyId = #{companyId},
      </if>
      <if test="itemTypeId != null" >
        itemTypeId = #{itemTypeId},
      </if>
      <if test="itemTypeName != null" >
        itemTypeName = #{itemTypeName},
      </if>
      <if test="specification != null" >
        specification = #{specification},
      </if>
      <if test="description != null" >
        description = #{description},
      </if>
      <if test="unit != null" >
        unit = #{unit},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE stock_type SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE stock_type SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.finance.StockType" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO stock_type
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      type,
      companyId,
      itemTypeId,
      itemTypeName,
      specification,
      description,
      unit,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{type},
      #{companyId},
      #{itemTypeId},
      #{itemTypeName},
      #{specification},
      #{description},
      #{unit},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.finance.StockType" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO stock_type 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="type != null" >
        type,
      </if>
      <if test="companyId != null" >
        companyId,
      </if>
      <if test="itemTypeId != null" >
        itemTypeId,
      </if>
      <if test="itemTypeName != null" >
        itemTypeName,
      </if>
      <if test="specification != null" >
        specification,
      </if>
      <if test="description != null" >
        description,
      </if>
      <if test="unit != null" >
        unit,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="type != null" >
        #{type},
      </if>
      <if test="companyId != null" >
        #{companyId},
      </if>
      <if test="itemTypeId != null" >
        #{itemTypeId},
      </if>
      <if test="itemTypeName != null" >
        #{itemTypeName},
      </if>
      <if test="specification != null" >
        #{specification},
      </if>
      <if test="description != null" >
        #{description},
      </if>
      <if test="unit != null" >
        #{unit},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM stock_type WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM stock_type
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>