<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : re_task_assignment_method
  Comment  : 维护任务分配与维护方式关联
  Mapper   : com.welearn.mapper.ReTaskAssignmentMethodMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2019/1/3 13:23:40
-->
<mapper namespace="com.welearn.mapper.ReTaskAssignmentMethodMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    taskAssignmentId,
    methodId,
    isProcessed
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    re_task_assignment_method.id AS id,
    re_task_assignment_method.createdAt AS createdAt,
    re_task_assignment_method.updatedAt AS updatedAt,
    re_task_assignment_method.isEnable AS isEnable,
    re_task_assignment_method.taskAssignmentId AS taskAssignmentId,
    re_task_assignment_method.methodId AS methodId,
    re_task_assignment_method.isProcessed AS isProcessed
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.equipment.ReTaskAssignmentMethod" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 维护任务分配id -->
    <result column="taskAssignmentId" property="taskAssignmentId"/>
    <!-- Comment: 维护方式id -->
    <result column="methodId" property="methodId"/>
    <!-- Comment: 是否已经处理: 0-未处理 1-已处理 -->
    <result column="isProcessed" property="isProcessed"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.equipment.ReTaskAssignmentMethod" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM re_task_assignment_method
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.equipment.ReTaskAssignmentMethod" parameterType="com.welearn.entity.qo.equipment.ReTaskAssignmentMethodQueryCondition" >
    SELECT
      rtam.*
    FROM re_task_assignment_method rtam
    <if test="equipmentId != null" >
      join task_assignment ta on ta.id = rtam.taskAssignmentId and ta.isEnable = true and ta.equipmentId=#{equipmentId}
    </if>
      <where>
        <if test="id != null" >
          rtam.id = #{id}
        </if>
        <if test="createdAt != null" >
          AND rtam.createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND rtam.updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND rtam.isEnable = #{isEnable}
        </if>
        <if test="taskAssignmentId != null" >
          AND rtam.taskAssignmentId = #{taskAssignmentId}
        </if>
        <if test="methodId != null" >
          AND rtam.methodId = #{methodId}
        </if>
        <if test="isProcessed != null" >
          AND rtam.isProcessed = #{isProcessed}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.equipment.ReTaskAssignmentMethod" >
    SELECT
      <include refid="sqlColumns"/>
    FROM re_task_assignment_method
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM re_task_assignment_method
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.equipment.ReTaskAssignmentMethod" >
    UPDATE re_task_assignment_method
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      taskAssignmentId = #{taskAssignmentId},
      methodId = #{methodId},
      isProcessed = #{isProcessed},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.equipment.ReTaskAssignmentMethod" >
    UPDATE re_task_assignment_method
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="taskAssignmentId != null" >
        taskAssignmentId = #{taskAssignmentId},
      </if>
      <if test="methodId != null" >
        methodId = #{methodId},
      </if>
      <if test="isProcessed != null" >
        isProcessed = #{isProcessed},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE re_task_assignment_method SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE re_task_assignment_method SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.equipment.ReTaskAssignmentMethod" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_task_assignment_method
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      taskAssignmentId,
      methodId,
      isProcessed,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{taskAssignmentId},
      #{methodId},
      #{isProcessed},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.equipment.ReTaskAssignmentMethod" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_task_assignment_method 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="taskAssignmentId != null" >
        taskAssignmentId,
      </if>
      <if test="methodId != null" >
        methodId,
      </if>
      <if test="isProcessed != null" >
        isProcessed,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="taskAssignmentId != null" >
        #{taskAssignmentId},
      </if>
      <if test="methodId != null" >
        #{methodId},
      </if>
      <if test="isProcessed != null" >
        #{isProcessed},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM re_task_assignment_method WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM re_task_assignment_method
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>