<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : re_maintenance_task_method
  Comment  : 维护任务与维护方式关联
  Mapper   : com.welearn.mapper.ReMaintenanceTaskMethodMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2019/1/3 13:23:39
-->
<mapper namespace="com.welearn.mapper.ReMaintenanceTaskMethodMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    taskId,
    methodId
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    re_maintenance_task_method.id AS id,
    re_maintenance_task_method.createdAt AS createdAt,
    re_maintenance_task_method.updatedAt AS updatedAt,
    re_maintenance_task_method.isEnable AS isEnable,
    re_maintenance_task_method.taskId AS taskId,
    re_maintenance_task_method.methodId AS methodId
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.equipment.ReMaintenanceTaskMethod" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 维护任务id -->
    <result column="taskId" property="taskId"/>
    <!-- Comment: 维护方式id -->
    <result column="methodId" property="methodId"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.equipment.ReMaintenanceTaskMethod" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM re_maintenance_task_method
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.equipment.ReMaintenanceTaskMethod" parameterType="com.welearn.entity.qo.equipment.ReMaintenanceTaskMethodQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM re_maintenance_task_method
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="taskId != null" >
          AND taskId = #{taskId}
        </if>
        <if test="methodId != null" >
          AND methodId = #{methodId}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.equipment.ReMaintenanceTaskMethod" >
    SELECT
      <include refid="sqlColumns"/>
    FROM re_maintenance_task_method
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM re_maintenance_task_method
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.equipment.ReMaintenanceTaskMethod" >
    UPDATE re_maintenance_task_method
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      taskId = #{taskId},
      methodId = #{methodId},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.equipment.ReMaintenanceTaskMethod" >
    UPDATE re_maintenance_task_method
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="taskId != null" >
        taskId = #{taskId},
      </if>
      <if test="methodId != null" >
        methodId = #{methodId},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE re_maintenance_task_method SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE re_maintenance_task_method SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.equipment.ReMaintenanceTaskMethod" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_maintenance_task_method
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      taskId,
      methodId,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{taskId},
      #{methodId},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.equipment.ReMaintenanceTaskMethod" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_maintenance_task_method 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="taskId != null" >
        taskId,
      </if>
      <if test="methodId != null" >
        methodId,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="taskId != null" >
        #{taskId},
      </if>
      <if test="methodId != null" >
        #{methodId},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM re_maintenance_task_method WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM re_maintenance_task_method
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>