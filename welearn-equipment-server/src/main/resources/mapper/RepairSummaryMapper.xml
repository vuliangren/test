<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : repair_summary
  Comment  : 维修总结
  Mapper   : com.welearn.mapper.RepairSummaryMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2019/2/12 17:14:04
-->
<mapper namespace="com.welearn.mapper.RepairSummaryMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    requestId,
    engineerId,
    engineerName,
    equipmentId,
    equipmentName,
    description,
    reason,
    method,
    handleRecord,
    engineerAdvice,
    reporterAdvice
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    repair_summary.id AS id,
    repair_summary.createdAt AS createdAt,
    repair_summary.updatedAt AS updatedAt,
    repair_summary.isEnable AS isEnable,
    repair_summary.requestId AS requestId,
    repair_summary.engineerId AS engineerId,
    repair_summary.engineerName AS engineerName,
    repair_summary.equipmentId AS equipmentId,
    repair_summary.equipmentName AS equipmentName,
    repair_summary.description AS description,
    repair_summary.reason AS reason,
    repair_summary.method AS method,
    repair_summary.handleRecord AS handleRecord,
    repair_summary.engineerAdvice AS engineerAdvice,
    repair_summary.reporterAdvice AS reporterAdvice
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.equipment.RepairSummary" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 报修id -->
    <result column="requestId" property="requestId"/>
    <!-- Comment: 工程师id -->
    <result column="engineerId" property="engineerId"/>
    <!-- Comment: 工程师名称 -->
    <result column="engineerName" property="engineerName"/>
    <!-- Comment: 设备id -->
    <result column="equipmentId" property="equipmentId"/>
    <!-- Comment: 设备名称 -->
    <result column="equipmentName" property="equipmentName"/>
    <!-- Comment: 故障描述(富文本) -->
    <result column="description" property="description"/>
    <!-- Comment: 起因分析(富文本) -->
    <result column="reason" property="reason"/>
    <!-- Comment: 维修方式: 0-厂家保修 1-自主维修 2-外援维修 3-其它 -->
    <result column="method" property="method"/>
    <!-- Comment: 处理方法(富文本) -->
    <result column="handleRecord" property="handleRecord"/>
    <!-- Comment: 给维修方建议 -->
    <result column="engineerAdvice" property="engineerAdvice"/>
    <!-- Comment: 给报修方建议 -->
    <result column="reporterAdvice" property="reporterAdvice"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.equipment.RepairSummary" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM repair_summary
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.equipment.RepairSummary" parameterType="com.welearn.entity.qo.equipment.RepairSummaryQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM repair_summary
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="requestId != null" >
          AND requestId = #{requestId}
        </if>
        <if test="engineerId != null" >
          AND engineerId = #{engineerId}
        </if>
        <if test="engineerName != null" >
          AND engineerName = #{engineerName}
        </if>
        <if test="equipmentId != null" >
          AND equipmentId = #{equipmentId}
        </if>
        <if test="equipmentName != null" >
          AND equipmentName = #{equipmentName}
        </if>
        <if test="description != null" >
          AND description = #{description}
        </if>
        <if test="reason != null" >
          AND reason = #{reason}
        </if>
        <if test="method != null" >
          AND method = #{method}
        </if>
        <if test="handleRecord != null" >
          AND handleRecord = #{handleRecord}
        </if>
        <if test="engineerAdvice != null" >
          AND engineerAdvice = #{engineerAdvice}
        </if>
        <if test="reporterAdvice != null" >
          AND reporterAdvice = #{reporterAdvice}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.equipment.RepairSummary" >
    SELECT
      <include refid="sqlColumns"/>
    FROM repair_summary
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM repair_summary
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.equipment.RepairSummary" >
    UPDATE repair_summary
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      requestId = #{requestId},
      engineerId = #{engineerId},
      engineerName = #{engineerName},
      equipmentId = #{equipmentId},
      equipmentName = #{equipmentName},
      description = #{description},
      reason = #{reason},
      method = #{method},
      handleRecord = #{handleRecord},
      engineerAdvice = #{engineerAdvice},
      reporterAdvice = #{reporterAdvice},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.equipment.RepairSummary" >
    UPDATE repair_summary
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="requestId != null" >
        requestId = #{requestId},
      </if>
      <if test="engineerId != null" >
        engineerId = #{engineerId},
      </if>
      <if test="engineerName != null" >
        engineerName = #{engineerName},
      </if>
      <if test="equipmentId != null" >
        equipmentId = #{equipmentId},
      </if>
      <if test="equipmentName != null" >
        equipmentName = #{equipmentName},
      </if>
      <if test="description != null" >
        description = #{description},
      </if>
      <if test="reason != null" >
        reason = #{reason},
      </if>
      <if test="method != null" >
        method = #{method},
      </if>
      <if test="handleRecord != null" >
        handleRecord = #{handleRecord},
      </if>
      <if test="engineerAdvice != null" >
        engineerAdvice = #{engineerAdvice},
      </if>
      <if test="reporterAdvice != null" >
        reporterAdvice = #{reporterAdvice},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE repair_summary SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE repair_summary SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.equipment.RepairSummary" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO repair_summary
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      requestId,
      engineerId,
      engineerName,
      equipmentId,
      equipmentName,
      description,
      reason,
      method,
      handleRecord,
      engineerAdvice,
      reporterAdvice,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{requestId},
      #{engineerId},
      #{engineerName},
      #{equipmentId},
      #{equipmentName},
      #{description},
      #{reason},
      #{method},
      #{handleRecord},
      #{engineerAdvice},
      #{reporterAdvice},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.equipment.RepairSummary" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO repair_summary 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="requestId != null" >
        requestId,
      </if>
      <if test="engineerId != null" >
        engineerId,
      </if>
      <if test="engineerName != null" >
        engineerName,
      </if>
      <if test="equipmentId != null" >
        equipmentId,
      </if>
      <if test="equipmentName != null" >
        equipmentName,
      </if>
      <if test="description != null" >
        description,
      </if>
      <if test="reason != null" >
        reason,
      </if>
      <if test="method != null" >
        method,
      </if>
      <if test="handleRecord != null" >
        handleRecord,
      </if>
      <if test="engineerAdvice != null" >
        engineerAdvice,
      </if>
      <if test="reporterAdvice != null" >
        reporterAdvice,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="requestId != null" >
        #{requestId},
      </if>
      <if test="engineerId != null" >
        #{engineerId},
      </if>
      <if test="engineerName != null" >
        #{engineerName},
      </if>
      <if test="equipmentId != null" >
        #{equipmentId},
      </if>
      <if test="equipmentName != null" >
        #{equipmentName},
      </if>
      <if test="description != null" >
        #{description},
      </if>
      <if test="reason != null" >
        #{reason},
      </if>
      <if test="method != null" >
        #{method},
      </if>
      <if test="handleRecord != null" >
        #{handleRecord},
      </if>
      <if test="engineerAdvice != null" >
        #{engineerAdvice},
      </if>
      <if test="reporterAdvice != null" >
        #{reporterAdvice},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM repair_summary WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM repair_summary
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>