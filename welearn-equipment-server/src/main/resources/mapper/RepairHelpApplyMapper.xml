<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : repair_help_apply
  Comment  : 维修外援申请
  Mapper   : com.welearn.mapper.RepairHelpApplyMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2019/3/11 14:37:00
-->
<mapper namespace="com.welearn.mapper.RepairHelpApplyMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    requestId,
    dispatchId,
    engineerId,
    engineerName,
    applyId,
    description,
    status
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    repair_help_apply.id AS id,
    repair_help_apply.createdAt AS createdAt,
    repair_help_apply.updatedAt AS updatedAt,
    repair_help_apply.isEnable AS isEnable,
    repair_help_apply.requestId AS requestId,
    repair_help_apply.dispatchId AS dispatchId,
    repair_help_apply.engineerId AS engineerId,
    repair_help_apply.engineerName AS engineerName,
    repair_help_apply.applyId AS applyId,
    repair_help_apply.description AS description,
    repair_help_apply.status AS status
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.equipment.RepairHelpApply" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 报修id -->
    <result column="requestId" property="requestId"/>
    <!-- Comment: 派工id -->
    <result column="dispatchId" property="dispatchId"/>
    <!-- Comment: 工程师id -->
    <result column="engineerId" property="engineerId"/>
    <!-- Comment: 工程师名称 -->
    <result column="engineerName" property="engineerName"/>
    <!-- Comment: 申请id -->
    <result column="applyId" property="applyId"/>
    <!-- Comment: 外援申请说明 -->
    <result column="description" property="description"/>
    <!-- Comment: 状态：0-待审批 1-审批通过 2-审批失败 -->
    <result column="status" property="status"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.equipment.RepairHelpApply" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM repair_help_apply
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.equipment.RepairHelpApply" parameterType="com.welearn.entity.qo.equipment.RepairHelpApplyQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM repair_help_apply
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="requestId != null" >
          AND requestId = #{requestId}
        </if>
        <if test="dispatchId != null" >
          AND dispatchId = #{dispatchId}
        </if>
        <if test="engineerId != null" >
          AND engineerId = #{engineerId}
        </if>
        <if test="engineerName != null" >
          AND engineerName = #{engineerName}
        </if>
        <if test="applyId != null" >
          AND applyId = #{applyId}
        </if>
        <if test="description != null" >
          AND description = #{description}
        </if>
        <if test="status != null" >
          AND status = #{status}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.equipment.RepairHelpApply" >
    SELECT
      <include refid="sqlColumns"/>
    FROM repair_help_apply
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM repair_help_apply
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.equipment.RepairHelpApply" >
    UPDATE repair_help_apply
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      requestId = #{requestId},
      dispatchId = #{dispatchId},
      engineerId = #{engineerId},
      engineerName = #{engineerName},
      applyId = #{applyId},
      description = #{description},
      status = #{status},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.equipment.RepairHelpApply" >
    UPDATE repair_help_apply
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="requestId != null" >
        requestId = #{requestId},
      </if>
      <if test="dispatchId != null" >
        dispatchId = #{dispatchId},
      </if>
      <if test="engineerId != null" >
        engineerId = #{engineerId},
      </if>
      <if test="engineerName != null" >
        engineerName = #{engineerName},
      </if>
      <if test="applyId != null" >
        applyId = #{applyId},
      </if>
      <if test="description != null" >
        description = #{description},
      </if>
      <if test="status != null" >
        status = #{status},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE repair_help_apply SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE repair_help_apply SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.equipment.RepairHelpApply" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO repair_help_apply
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      requestId,
      dispatchId,
      engineerId,
      engineerName,
      applyId,
      description,
      status,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{requestId},
      #{dispatchId},
      #{engineerId},
      #{engineerName},
      #{applyId},
      #{description},
      #{status},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.equipment.RepairHelpApply" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO repair_help_apply 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="requestId != null" >
        requestId,
      </if>
      <if test="dispatchId != null" >
        dispatchId,
      </if>
      <if test="engineerId != null" >
        engineerId,
      </if>
      <if test="engineerName != null" >
        engineerName,
      </if>
      <if test="applyId != null" >
        applyId,
      </if>
      <if test="description != null" >
        description,
      </if>
      <if test="status != null" >
        status,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="requestId != null" >
        #{requestId},
      </if>
      <if test="dispatchId != null" >
        #{dispatchId},
      </if>
      <if test="engineerId != null" >
        #{engineerId},
      </if>
      <if test="engineerName != null" >
        #{engineerName},
      </if>
      <if test="applyId != null" >
        #{applyId},
      </if>
      <if test="description != null" >
        #{description},
      </if>
      <if test="status != null" >
        #{status},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM repair_help_apply WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM repair_help_apply
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>