<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : repair_interrupt
  Comment  : 报修中止申请
  Mapper   : com.welearn.mapper.RepairInterruptMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2019/3/7 9:55:09
-->
<mapper namespace="com.welearn.mapper.RepairInterruptMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    requestId,
    applyId,
    equipmentId,
    dispatchId,
    engineerId,
    engineerName,
    reason,
    result
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    repair_interrupt.id AS id,
    repair_interrupt.createdAt AS createdAt,
    repair_interrupt.updatedAt AS updatedAt,
    repair_interrupt.isEnable AS isEnable,
    repair_interrupt.requestId AS requestId,
    repair_interrupt.applyId AS applyId,
    repair_interrupt.equipmentId AS equipmentId,
    repair_interrupt.dispatchId AS dispatchId,
    repair_interrupt.engineerId AS engineerId,
    repair_interrupt.engineerName AS engineerName,
    repair_interrupt.reason AS reason,
    repair_interrupt.result AS result
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.equipment.RepairInterrupt" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 报修id -->
    <result column="requestId" property="requestId"/>
    <!-- Comment: 申请id -->
    <result column="applyId" property="applyId"/>
    <!-- Comment: 设备id -->
    <result column="equipmentId" property="equipmentId"/>
    <!-- Comment: 内部派工id -->
    <result column="dispatchId" property="dispatchId"/>
    <!-- Comment: 工程师id -->
    <result column="engineerId" property="engineerId"/>
    <!-- Comment: 工程师名称 -->
    <result column="engineerName" property="engineerName"/>
    <!-- Comment: 中止原因 -->
    <result column="reason" property="reason"/>
    <!-- Comment: 审批结果：0-通过 1-驳回 -->
    <result column="result" property="result"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.equipment.RepairInterrupt" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM repair_interrupt
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.equipment.RepairInterrupt" parameterType="com.welearn.entity.qo.equipment.RepairInterruptQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM repair_interrupt
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="requestId != null" >
          AND requestId = #{requestId}
        </if>
        <if test="applyId != null" >
          AND applyId = #{applyId}
        </if>
        <if test="equipmentId != null" >
          AND equipmentId = #{equipmentId}
        </if>
        <if test="dispatchId != null" >
          AND dispatchId = #{dispatchId}
        </if>
        <if test="engineerId != null" >
          AND engineerId = #{engineerId}
        </if>
        <if test="engineerName != null" >
          AND engineerName = #{engineerName}
        </if>
        <if test="reason != null" >
          AND reason = #{reason}
        </if>
        <if test="result != null" >
          AND result = #{result}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.equipment.RepairInterrupt" >
    SELECT
      <include refid="sqlColumns"/>
    FROM repair_interrupt
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM repair_interrupt
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.equipment.RepairInterrupt" >
    UPDATE repair_interrupt
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      requestId = #{requestId},
      applyId = #{applyId},
      equipmentId = #{equipmentId},
      dispatchId = #{dispatchId},
      engineerId = #{engineerId},
      engineerName = #{engineerName},
      reason = #{reason},
      result = #{result},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.equipment.RepairInterrupt" >
    UPDATE repair_interrupt
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="requestId != null" >
        requestId = #{requestId},
      </if>
      <if test="applyId != null" >
        applyId = #{applyId},
      </if>
      <if test="equipmentId != null" >
        equipmentId = #{equipmentId},
      </if>
      <if test="dispatchId != null" >
        dispatchId = #{dispatchId},
      </if>
      <if test="engineerId != null" >
        engineerId = #{engineerId},
      </if>
      <if test="engineerName != null" >
        engineerName = #{engineerName},
      </if>
      <if test="reason != null" >
        reason = #{reason},
      </if>
      <if test="result != null" >
        result = #{result},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE repair_interrupt SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE repair_interrupt SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.equipment.RepairInterrupt" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO repair_interrupt
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      requestId,
      applyId,
      equipmentId,
      dispatchId,
      engineerId,
      engineerName,
      reason,
      result,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{requestId},
      #{applyId},
      #{equipmentId},
      #{dispatchId},
      #{engineerId},
      #{engineerName},
      #{reason},
      #{result},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.equipment.RepairInterrupt" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO repair_interrupt 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="requestId != null" >
        requestId,
      </if>
      <if test="applyId != null" >
        applyId,
      </if>
      <if test="equipmentId != null" >
        equipmentId,
      </if>
      <if test="dispatchId != null" >
        dispatchId,
      </if>
      <if test="engineerId != null" >
        engineerId,
      </if>
      <if test="engineerName != null" >
        engineerName,
      </if>
      <if test="reason != null" >
        reason,
      </if>
      <if test="result != null" >
        result,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="requestId != null" >
        #{requestId},
      </if>
      <if test="applyId != null" >
        #{applyId},
      </if>
      <if test="equipmentId != null" >
        #{equipmentId},
      </if>
      <if test="dispatchId != null" >
        #{dispatchId},
      </if>
      <if test="engineerId != null" >
        #{engineerId},
      </if>
      <if test="engineerName != null" >
        #{engineerName},
      </if>
      <if test="reason != null" >
        #{reason},
      </if>
      <if test="result != null" >
        #{result},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM repair_interrupt WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM repair_interrupt
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>