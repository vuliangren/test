<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : upload_record
  Comment  : 文件上传记录
  Mapper   : com.welearn.mapper.UploadRecordMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2018/9/30 9:36:19
-->
<mapper namespace="com.welearn.mapper.UploadRecordMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    name,
    size,
    suffix,
    mimeType,
    `key`,
    `hash`,
    bucket,
    creatorId
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    upload_record.id AS id,
    upload_record.createdAt AS createdAt,
    upload_record.updatedAt AS updatedAt,
    upload_record.isEnable AS isEnable,
    upload_record.name AS name,
    upload_record.size AS size,
    upload_record.suffix AS suffix,
    upload_record.mimeType AS mimeType,
    upload_record.key AS `key`,
    upload_record.hash AS `hash`,
    upload_record.bucket AS bucket,
    upload_record.creatorId AS creatorId
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.storage.UploadRecord" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 文件名称 -->
    <result column="name" property="name"/>
    <!-- Comment: 文件大小 -->
    <result column="size" property="size"/>
    <!-- Comment: 文件后缀 -->
    <result column="suffix" property="suffix"/>
    <!-- Comment: MIME类型 -->
    <result column="mimeType" property="mimeType"/>
    <!-- Comment: 文件key -->
    <result column="key" property="key"/>
    <!-- Comment: 文件hash -->
    <result column="hash" property="hash"/>
    <!-- Comment: 所在空间 -->
    <result column="bucket" property="bucket"/>
    <!-- Comment: 创建人id -->
    <result column="creatorId" property="creatorId"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.storage.UploadRecord" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM upload_record
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.storage.UploadRecord" parameterType="com.welearn.entity.qo.storage.UploadRecordQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM upload_record
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="name != null" >
          AND name = #{name}
        </if>
        <if test="size != null" >
          AND size = #{size}
        </if>
        <if test="suffix != null" >
          AND suffix = #{suffix}
        </if>
        <if test="mimeType != null" >
          AND mimeType = #{mimeType}
        </if>
        <if test="key != null" >
          AND `key` = #{key}
        </if>
        <if test="hash != null" >
          AND `hash` = #{hash}
        </if>
        <if test="bucket != null" >
          AND bucket = #{bucket}
        </if>
        <if test="creatorId != null" >
          AND creatorId = #{creatorId}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.storage.UploadRecord" >
    SELECT
      <include refid="sqlColumns"/>
    FROM upload_record
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM upload_record
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.storage.UploadRecord" >
    UPDATE upload_record
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      name = #{name},
      size = #{size},
      suffix = #{suffix},
      mimeType = #{mimeType},
      `key` = #{key},
      `hash` = #{hash},
      bucket = #{bucket},
      creatorId = #{creatorId},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.storage.UploadRecord" >
    UPDATE upload_record
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="name != null" >
        name = #{name},
      </if>
      <if test="size != null" >
        size = #{size},
      </if>
      <if test="suffix != null" >
        suffix = #{suffix},
      </if>
      <if test="mimeType != null" >
        mimeType = #{mimeType},
      </if>
      <if test="key != null" >
        `key` = #{key},
      </if>
      <if test="hash != null" >
        `hash` = #{hash},
      </if>
      <if test="bucket != null" >
        bucket = #{bucket},
      </if>
      <if test="creatorId != null" >
        creatorId = #{creatorId},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE upload_record SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE upload_record SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.storage.UploadRecord" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO upload_record
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      name,
      size,
      suffix,
      mimeType,
      `key`,
      `hash`,
      bucket,
      creatorId,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{name},
      #{size},
      #{suffix},
      #{mimeType},
      #{key},
      #{hash},
      #{bucket},
      #{creatorId},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.storage.UploadRecord" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO upload_record 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="name != null" >
        name,
      </if>
      <if test="size != null" >
        size,
      </if>
      <if test="suffix != null" >
        suffix,
      </if>
      <if test="mimeType != null" >
        mimeType,
      </if>
      <if test="key != null" >
        `key`,
      </if>
      <if test="hash != null" >
        `hash`,
      </if>
      <if test="bucket != null" >
        bucket,
      </if>
      <if test="creatorId != null" >
        creatorId,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="name != null" >
        #{name},
      </if>
      <if test="size != null" >
        #{size},
      </if>
      <if test="suffix != null" >
        #{suffix},
      </if>
      <if test="mimeType != null" >
        #{mimeType},
      </if>
      <if test="key != null" >
        #{key},
      </if>
      <if test="hash != null" >
        #{hash},
      </if>
      <if test="bucket != null" >
        #{bucket},
      </if>
      <if test="creatorId != null" >
        #{creatorId},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM upload_record WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM upload_record
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>