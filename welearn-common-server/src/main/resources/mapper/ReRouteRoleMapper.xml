<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
  Table    : re_route_role
  Comment  : 前端路由与角色编码的关联
  Mapper   : com.welearn.mapper.ReRouteRoleMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2019/3/4 18:00:33
-->
<mapper namespace="com.welearn.mapper.ReRouteRoleMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    path,
    role,
    clientType
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    re_route_role.id AS id,
    re_route_role.createdAt AS createdAt,
    re_route_role.updatedAt AS updatedAt,
    re_route_role.isEnable AS isEnable,
    re_route_role.path AS path,
    re_route_role.role AS role,
    re_route_role.clientType AS clientType
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.common.ReRouteRole" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 前端路由地址 -->
    <result column="path" property="path"/>
    <!-- Comment: 角色code -->
    <result column="role" property="role"/>
    <!-- Comment: 客户端类型: 1-web -->
    <result column="clientType" property="clientType"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.common.ReRouteRole" parameterType="java.lang.String">
    SELECT
    <include refid="sqlColumns"/>
    FROM re_route_role
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.common.ReRouteRole" parameterType="com.welearn.entity.qo.common.ReRouteRoleQueryCondition" >
    SELECT
    <include refid="sqlColumns"/>
    FROM re_route_role
    <where>
      <if test="id != null" >
        id = #{id}
      </if>
      <if test="createdAt != null" >
        AND createdAt = #{createdAt}
      </if>
      <if test="updatedAt != null" >
        AND updatedAt = #{updatedAt}
      </if>
      <if test="isEnable != null" >
        AND isEnable = #{isEnable}
      </if>
      <if test="path != null" >
        AND path = #{path}
      </if>
      <if test="role != null" >
        AND role = #{role}
      </if>
      <if test="clientType != null" >
        AND clientType = #{clientType}
      </if>
      <!-- START USER DEFINED -->

      <!-- End USER DEFINED -->
    </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.common.ReRouteRole" >
    SELECT
    <include refid="sqlColumns"/>
    FROM re_route_role
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM re_route_role
  </select>

  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.common.ReRouteRole" >
    UPDATE re_route_role
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      path = #{path},
      role = #{role},
      clientType = #{clientType},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.common.ReRouteRole" >
    UPDATE re_route_role
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="path != null" >
        path = #{path},
      </if>
      <if test="role != null" >
        role = #{role},
      </if>
      <if test="clientType != null" >
        clientType = #{clientType},
      </if>
    </set>
    WHERE id = #{id}
  </update>

  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE re_route_role SET isEnable = TRUE WHERE id = #{id}
  </update>

  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE re_route_role SET isEnable = FALSE WHERE id = #{id}
  </update>

  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.common.ReRouteRole" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_route_role
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      path,
      role,
      clientType,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{path},
      #{role},
      #{clientType},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.common.ReRouteRole" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_route_role
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="path != null" >
        path,
      </if>
      <if test="role != null" >
        role,
      </if>
      <if test="clientType != null" >
        clientType,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="path != null" >
        #{path},
      </if>
      <if test="role != null" >
        #{role},
      </if>
      <if test="clientType != null" >
        #{clientType},
      </if>
    </trim>
  </insert>

  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM re_route_role WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM re_route_role
  </delete>


  <!-- START USER DEFINED -->
  <select id="selectAllDistinctPathAndRole" resultType="com.welearn.entity.dto.common.RouteRole">
    select distinct rrr.path, rrr.role
    from re_route_role rrr
    where rrr.isEnable = true and rrr.clientType = #{clientType}
  </select>
</mapper>