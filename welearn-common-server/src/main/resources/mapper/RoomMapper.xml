<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : room
  Comment  : 房间
  Mapper   : com.welearn.mapper.RoomMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2019/3/16 18:24:38
-->
<mapper namespace="com.welearn.mapper.RoomMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    creatorId,
    companyId,
    buildingId,
    floorId,
    name,
    description
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    room.id AS id,
    room.createdAt AS createdAt,
    room.updatedAt AS updatedAt,
    room.isEnable AS isEnable,
    room.creatorId AS creatorId,
    room.companyId AS companyId,
    room.buildingId AS buildingId,
    room.floorId AS floorId,
    room.name AS name,
    room.description AS description
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.common.Room" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 创建者id -->
    <result column="creatorId" property="creatorId"/>
    <!-- Comment: 公司id -->
    <result column="companyId" property="companyId"/>
    <!-- Comment: 建筑id -->
    <result column="buildingId" property="buildingId"/>
    <!-- Comment: 楼层id -->
    <result column="floorId" property="floorId"/>
    <!-- Comment: 名称(门牌号) -->
    <result column="name" property="name"/>
    <!-- Comment: 描述 -->
    <result column="description" property="description"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.common.Room" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM room
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.common.Room" parameterType="com.welearn.entity.qo.common.RoomQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM room
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="creatorId != null" >
          AND creatorId = #{creatorId}
        </if>
        <if test="companyId != null" >
          AND companyId = #{companyId}
        </if>
        <if test="buildingId != null" >
          AND buildingId = #{buildingId}
        </if>
        <if test="floorId != null" >
          AND floorId = #{floorId}
        </if>
        <if test="name != null" >
          AND name = #{name}
        </if>
        <if test="description != null" >
          AND description = #{description}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.common.Room" >
    SELECT
      <include refid="sqlColumns"/>
    FROM room
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM room
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.common.Room" >
    UPDATE room
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      creatorId = #{creatorId},
      companyId = #{companyId},
      buildingId = #{buildingId},
      floorId = #{floorId},
      name = #{name},
      description = #{description},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.common.Room" >
    UPDATE room
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="creatorId != null" >
        creatorId = #{creatorId},
      </if>
      <if test="companyId != null" >
        companyId = #{companyId},
      </if>
      <if test="buildingId != null" >
        buildingId = #{buildingId},
      </if>
      <if test="floorId != null" >
        floorId = #{floorId},
      </if>
      <if test="name != null" >
        name = #{name},
      </if>
      <if test="description != null" >
        description = #{description},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE room SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE room SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.common.Room" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO room
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      creatorId,
      companyId,
      buildingId,
      floorId,
      name,
      description,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{creatorId},
      #{companyId},
      #{buildingId},
      #{floorId},
      #{name},
      #{description},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.common.Room" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO room 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="creatorId != null" >
        creatorId,
      </if>
      <if test="companyId != null" >
        companyId,
      </if>
      <if test="buildingId != null" >
        buildingId,
      </if>
      <if test="floorId != null" >
        floorId,
      </if>
      <if test="name != null" >
        name,
      </if>
      <if test="description != null" >
        description,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="creatorId != null" >
        #{creatorId},
      </if>
      <if test="companyId != null" >
        #{companyId},
      </if>
      <if test="buildingId != null" >
        #{buildingId},
      </if>
      <if test="floorId != null" >
        #{floorId},
      </if>
      <if test="name != null" >
        #{name},
      </if>
      <if test="description != null" >
        #{description},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM room WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM room
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>