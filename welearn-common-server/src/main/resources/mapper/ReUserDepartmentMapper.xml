<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 
  Table    : re_user_department
  Comment  : 用户部门入离职记录
  Mapper   : com.welearn.mapper.ReUserDepartmentMapper
  Author   : Setsuna Jin build with CodeSmith 7.0
  CreateAt : 2019/5/7 16:07:00
-->
<mapper namespace="com.welearn.mapper.ReUserDepartmentMapper">
  <!-- SQL 片段:所有字段 -->
  <sql id="sqlColumns" >
    id,
    createdAt,
    updatedAt,
    isEnable,
    creatorId,
    companyId,
    departmentId,
    userId,
    entryRemark,
    leaveRemark
  </sql>

  <!-- SQL 片段:所有字段（带表头和 AS） -->
  <sql id="sqlColumnsAs" >
    re_user_department.id AS id,
    re_user_department.createdAt AS createdAt,
    re_user_department.updatedAt AS updatedAt,
    re_user_department.isEnable AS isEnable,
    re_user_department.creatorId AS creatorId,
    re_user_department.companyId AS companyId,
    re_user_department.departmentId AS departmentId,
    re_user_department.userId AS userId,
    re_user_department.entryRemark AS entryRemark,
    re_user_department.leaveRemark AS leaveRemark
  </sql>

  <!-- ResultMap: 默认表结构 -->
  <resultMap id="resultMapDefault" type="com.welearn.entity.po.common.ReUserDepartment" >
    <!-- Comment: 主键 -->
    <id column="id" property="id"/>
    <!-- Comment: 创建时间 -->
    <result column="createdAt" property="createdAt"/>
    <!-- Comment: 更新时间 -->
    <result column="updatedAt" property="updatedAt"/>
    <!-- Comment: 是否可用：0-不可用，1-可用 -->
    <result column="isEnable" property="isEnable"/>
    <!-- Comment: 创建者id -->
    <result column="creatorId" property="creatorId"/>
    <!-- Comment: 公司id -->
    <result column="companyId" property="companyId"/>
    <!-- Comment: 部门id -->
    <result column="departmentId" property="departmentId"/>
    <!-- Comment: 用户id -->
    <result column="userId" property="userId"/>
    <!-- Comment: 入职备注 -->
    <result column="entryRemark" property="entryRemark"/>
    <!-- Comment: 离职备注 -->
    <result column="leaveRemark" property="leaveRemark"/>
  </resultMap>

  <!-- 按主键查询数据 -->
  <select id="selectByPK" resultType="com.welearn.entity.po.common.ReUserDepartment" parameterType="java.lang.String">
    SELECT
      <include refid="sqlColumns"/>
    FROM re_user_department
    WHERE id = #{id}
  </select>

  <!-- 按条件查询数据 -->
  <select id="selectByCondition" resultType="com.welearn.entity.po.common.ReUserDepartment" parameterType="com.welearn.entity.qo.common.ReUserDepartmentQueryCondition" >
    SELECT
      <include refid="sqlColumns"/>
    FROM re_user_department
      <where>
        <if test="id != null" >
          id = #{id}
        </if>
        <if test="createdAt != null" >
          AND createdAt = #{createdAt}
        </if>
        <if test="updatedAt != null" >
          AND updatedAt = #{updatedAt}
        </if>
        <if test="isEnable != null" >
          AND isEnable = #{isEnable}
        </if>
        <if test="creatorId != null" >
          AND creatorId = #{creatorId}
        </if>
        <if test="companyId != null" >
          AND companyId = #{companyId}
        </if>
        <if test="departmentId != null" >
          AND departmentId = #{departmentId}
        </if>
        <if test="userId != null" >
          AND userId = #{userId}
        </if>
        <if test="entryRemark != null" >
          AND entryRemark = #{entryRemark}
        </if>
        <if test="leaveRemark != null" >
          AND leaveRemark = #{leaveRemark}
        </if>
        <!-- START USER DEFINED -->
        
        <!-- End USER DEFINED -->
      </where>
    ORDER BY createdAt ASC, updatedAt DESC
  </select>

  <!-- 查询所有数据 -->
  <select id="selectAll" resultType="com.welearn.entity.po.common.ReUserDepartment" >
    SELECT
      <include refid="sqlColumns"/>
    FROM re_user_department
  </select>

  <!-- 获取数据条数 -->
  <select id="countAll" resultType="java.lang.Integer" >
    SELECT count(id)
    FROM re_user_department
  </select>
  
  <!-- 更新数据（全部） -->
  <update id="updateByPK" parameterType="com.welearn.entity.po.common.ReUserDepartment" >
    UPDATE re_user_department
    <set>
      createdAt = #{createdAt},
      updatedAt = #{updatedAt},
      isEnable = #{isEnable},
      creatorId = #{creatorId},
      companyId = #{companyId},
      departmentId = #{departmentId},
      userId = #{userId},
      entryRemark = #{entryRemark},
      leaveRemark = #{leaveRemark},
    </set>
    WHERE id = #{id}
  </update>

  <!-- 更新数据（选择） -->
  <update id="updateByPKSelective" parameterType="com.welearn.entity.po.common.ReUserDepartment" >
    UPDATE re_user_department
    <set>
      <if test="createdAt != null" >
        createdAt = #{createdAt},
      </if>
      <if test="updatedAt != null" >
        updatedAt = #{updatedAt},
      </if>
      <if test="isEnable != null" >
        isEnable = #{isEnable},
      </if>
      <if test="creatorId != null" >
        creatorId = #{creatorId},
      </if>
      <if test="companyId != null" >
        companyId = #{companyId},
      </if>
      <if test="departmentId != null" >
        departmentId = #{departmentId},
      </if>
      <if test="userId != null" >
        userId = #{userId},
      </if>
      <if test="entryRemark != null" >
        entryRemark = #{entryRemark},
      </if>
      <if test="leaveRemark != null" >
        leaveRemark = #{leaveRemark},
      </if>
    </set>
    WHERE id = #{id}
  </update>
  
  <!-- 启用 -->
  <update id="enable" parameterType="java.lang.String" >
    UPDATE re_user_department SET isEnable = TRUE WHERE id = #{id}
  </update>
  
  <!-- 禁用 -->
  <update id="disable" parameterType="java.lang.String" >
    UPDATE re_user_department SET isEnable = FALSE WHERE id = #{id}
  </update>
  
  <!-- 添加数据（全部） -->
  <insert id="insert" parameterType="com.welearn.entity.po.common.ReUserDepartment" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_user_department
    <trim prefix="(" suffix=")" suffixOverrides="," >
      id,
      createdAt,
      updatedAt,
      isEnable,
      creatorId,
      companyId,
      departmentId,
      userId,
      entryRemark,
      leaveRemark,
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      #{id},
      #{createdAt},
      #{updatedAt},
      #{isEnable},
      #{creatorId},
      #{companyId},
      #{departmentId},
      #{userId},
      #{entryRemark},
      #{leaveRemark},
    </trim>
  </insert>

  <!-- 添加数据（选择） -->
  <insert id="insertSelective" parameterType="com.welearn.entity.po.common.ReUserDepartment" useGeneratedKeys="true" keyProperty="id" >
    INSERT INTO re_user_department 
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="createdAt != null" >
        createdAt,
      </if>
      <if test="updatedAt != null" >
        updatedAt,
      </if>
      <if test="isEnable != null" >
        isEnable,
      </if>
      <if test="creatorId != null" >
        creatorId,
      </if>
      <if test="companyId != null" >
        companyId,
      </if>
      <if test="departmentId != null" >
        departmentId,
      </if>
      <if test="userId != null" >
        userId,
      </if>
      <if test="entryRemark != null" >
        entryRemark,
      </if>
      <if test="leaveRemark != null" >
        leaveRemark,
      </if>
    </trim>
    <trim prefix="VALUES (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id},
      </if>
      <if test="createdAt != null" >
        #{createdAt},
      </if>
      <if test="updatedAt != null" >
        #{updatedAt},
      </if>
      <if test="isEnable != null" >
        #{isEnable},
      </if>
      <if test="creatorId != null" >
        #{creatorId},
      </if>
      <if test="companyId != null" >
        #{companyId},
      </if>
      <if test="departmentId != null" >
        #{departmentId},
      </if>
      <if test="userId != null" >
        #{userId},
      </if>
      <if test="entryRemark != null" >
        #{entryRemark},
      </if>
      <if test="leaveRemark != null" >
        #{leaveRemark},
      </if>
    </trim>
  </insert>
  
  <!-- 根据主键删除 -->
  <delete id="deleteByPK" parameterType="java.lang.String" >
    DELETE FROM re_user_department WHERE id = #{id}
  </delete>

  <!-- 删除全部数据 -->
  <delete id="deleteAll">
    DELETE FROM re_user_department
  </delete>
  
  
  <!-- START USER DEFINED -->
  
</mapper>